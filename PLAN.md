# ProxySentry 下一版本计划

## 用户可见结果

当网络异常时，面板尽量给出以下五类结论，而不是笼统显示代理异常：

1. 直连与基础网络正常。
2. 电脑代理接管或本地 Clash 配置异常。
3. 当前代理节点异常，但同机场备选节点正常。
4. 同机场多个节点同时异常，疑似机场侧故障。
5. 本地无线网络、路由器、域名解析或外部运营商网络异常。

“route”改为更直接的“traffic”，区分两件事：

- 系统是否配置了代理接管；
- 是否实际观察到流量经过 Clash。

## 最小实现

### A. Mihomo 只读遥测

- 读取 `/connections`，只保留活动连接数量、经过非直连代理链的数量和累计字节数。
- 不保存或展示域名、地址、进程路径、规则内容、节点名和原始响应。
- 不依赖实时文本日志；日志缺失不能作为路由失败证据。
- 仅当当前节点检测失败时，最多检测同一代理提供方的两个备选节点。
- 备选检测只调用只读延迟接口，不切换节点、不修改配置、不持久化节点名。

### B. 基础网络分层

- 本地层：网络接口状态与默认网关可达性；网关不响应只能作为弱证据。
- 公网层：对两个固定公网地址建立不依赖域名解析的连接。
- 域名层：单独检测域名解析。
- 应用层：保留现有两个直连网页探测。
- 任何单一目标失败都不直接归因；至少使用两类独立证据。

### C. 诊断规则

- 公网地址可达、域名解析失败：电脑域名解析配置异常。
- 网关可达、公网地址持续失败：外部网络或运营商异常。
- 网络接口不可用，或网关与公网地址同时失败：本地网络或路由器异常。
- 直连正常、Clash 正常、节点正常，但固定代理出口失败：电脑代理配置异常。
- 当前节点失败、同提供方备选节点成功：当前节点异常。
- 当前节点与同提供方两个备选节点均失败，且直连和本地 Clash 正常：疑似机场侧异常。
- 证据不足、自动代理脚本未解析或虚拟网卡无法独立绕过时：保持未知，不强行归因。

### D. 面板

- 终端风格保留，主矩阵使用 `base / traffic / proxy / clash` 四行。
- `traffic ACTIVE` 表示观察到非直连 Clash 连接；`WAIT` 表示尚无证据，不表示失败。
- 异常区显示一条直接结论及最多两条证据。
- 面板继续支持点击外部自动收起，不增加常驻窗口。

## 并行所有权

- `pi_clash`：`ClashReader.swift`、`ClashReaderTests.swift`。
- `pi_core`：`NetworkProbes.swift`、`SystemNetwork.swift` 及对应测试。
- `pi_projectcheck`：`Diagnosis.swift`、`DiagnosticsController.swift` 及对应测试。
- 主代理：`ProxySentryApp.swift`、`StatusPopoverView.swift`、`SystemServices.swift`、视觉测试、集成与实机验收。

所有代理共享工作树，不得回退或覆盖其他代理的改动；遇到接口差异时适配现状并报告。

## 完成标准

- 所有新接口均为只读，原始连接、日志和节点数据不进入界面模型。
- 单元测试覆盖上述诊断矩阵、隐私边界、超时与未知状态。
- 完整测试通过，正常构建成功，Git 差异检查无错误。
- 实机确认至少能分别显示：Clash 正在承载流量、系统未全局接管、节点健康。
- 浅色、深色与异常溢出快照无裁切、重叠或缺失。
