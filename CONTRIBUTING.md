# 贡献指南

1. 先搜索现有 Issue；没有对应条目时，创建 Issue 描述背景、复现步骤和期望行为。
2. Fork 仓库，创建主题分支，提交范围清晰的小改动。
3. 本地运行相关测试，并检查 `git diff --check`。
4. 发起 Pull Request，说明改动、验证方式和已知限制。

请保持 ProxySentry 的只读隐私边界：不要新增遥测、上传原始连接或保存节点/secret；确有必要时请先在 Issue 中讨论。不要提交个人配置、订阅链接、密钥、日志或构建产物。

独立 `watchdog.rb` 是经用户明确启用后的有限例外：允许只为当前节点入口修复读取运行配置和当前订阅、生成私有临时配置与恢复备份、重载并验证；诊断主进程仍只读。辅助进程默认关闭，状态必须脱敏，取消/验证失败需回滚，外部修改不能被覆盖。测试只能使用临时合成配置，不得修改开发者本机 Clash 配置或自动启用自愈。

提交代码即表示你同意在本项目 Massachusetts Institute of Technology License（MIT License，麻省理工学院许可证，一种宽松开源许可证）下贡献这些改动。ProxySentry 是独立开源项目，与 Clash Verge Rev 及其维护者没有官方关联。
