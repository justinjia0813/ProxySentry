# ProxySentry 2.2.2 · 未签名预览版

- 修正规则模式下真实代理出口探测成功、总体却显示灰色的问题；系统自动代理规则未验证时保留明确说明。
- 规则模式的 `proxy` 行显示真实出口检测结果，全局模式仍显示所选节点测速。
- 直连模式或模式未知时，不因 Clash 混合端口请求成功而套用新增的代理正常判定。
- 移除看门狗设置、自动修复启动逻辑及随包辅助脚本，恢复纯只读诊断。

验证：268 项测试通过，规则模式界面已渲染检查；本地规则模式实测两个代理出口成功，总体显示绿色。

macOS 13+，Apple 芯片和 Intel 通用构建。发布包未获开发者签名或 Apple 公证，系统安全检查可能阻止启动；这是测试用预览版。
