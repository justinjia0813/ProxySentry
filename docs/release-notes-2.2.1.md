# ProxySentry 2.2.1 · 未签名预览版

- 展开面板独立显示 Clash 规则、全局、直连模式；读取不到时显示“模式未知”。
- 区分基础网络故障和代理出口失败；节点延迟正常但真实外站持续超时时报告“疑似入口拨号失败”。
- 已确认的诊断与证据同步更新，短暂波动不替换稳定结论；旧流量计数不掩盖本轮真实失败。
- 新增默认关闭的独立入口自愈：多解析器取样、隔离真实连接验证、私有备份、失败回滚、冷却防重放与旧看门狗冲突保护。

诊断主进程保持只读。自愈只有明确启用后才可修改当前节点的运行配置和当前订阅，仅支持全局模式中直接选中的 VLESS REALITY 节点；规则模式、直连模式、嵌套策略组及虚拟网卡场景不自动修复。不切换节点、不修改系统代理、不自动接管旧看门狗。备份包含原配置凭据，请勿上传或分享。

macOS 13+，Apple 芯片和 Intel 通用构建。下载包未获开发者签名或 Apple 公证，系统安全检查可能阻止启动；这是测试用预览版，不是正式免提示安装包。
